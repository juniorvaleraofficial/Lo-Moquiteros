// Practicar ejercicios del curso
// Realizar la suma de los primeros 5 numeros usando ciclos

// let contador = 0;
let n = 0;

for (contador = 1; contador <= 5; contador++){
    n+= contador;
    console.log(`ciclo for = ${n}`);
}
/*do{
    if (n >= 1 && n <= 15){
        console.log(`ciclo do while = ${n}`);
    }
    contador++;
    n+= contador;
}while (contador <= 5);
*/

/*while (contador <= 5) {
    if (n >= 1 && n <= 15){
        console.log(`ciclo while = ${n}`);
    }
    contador++;
    n+= contador;
}*/